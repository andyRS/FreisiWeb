'use strict'

window.addEventListener('load', ()=>{
    const contenedorLoader = document.querySelector('#pre-load');

    contenedorLoader.style.opacity= 0;
    contenedorLoader.style.visibility = 'hidden';

});